plugins {
    id "com.android.application"
}

android {
    namespace "com.xziel.engineprototype"
    compileSdk {
        version = release(36) {
            it.minorApiLevel = 1
        }
    }
    ndkVersion "28.2.13676358"

    defaultConfig {
        applicationId "com.xziel.engineprototype"
        minSdk 30
        targetSdk 36
        versionCode 1
        versionName "0.0.1-foundation"

        externalNativeBuild {
            cmake {
                cppFlags "-std=c++20 -Wall -Wextra -Wpedantic"
                arguments "-DANDROID_STL=c++_shared"
            }
        }

        ndk {
            abiFilters "arm64-v8a", "x86_64"
        }
    }

    buildFeatures {
        prefab true
    }

    externalNativeBuild {
        cmake {
            path file("src/main/cpp/CMakeLists.txt")
            version "3.22.1"
        }
    }

    buildTypes {
        debug {
            debuggable true
            jniDebuggable true
        }
        release {
            debuggable false
            jniDebuggable false
            minifyEnabled false
        }
    }

    androidResources {
        // KTX2 is already GPU block-compressed. Keep it STORED in the APK so
        // future mip streaming can range-read the asset without ZIP inflate.
        noCompress += ["ktx2", "xzsm"]
    }

    packaging {
        jniLibs {
            useLegacyPackaging false
        }
    }
}

dependencies {
    implementation "androidx.appcompat:appcompat:1.7.1"
    implementation "androidx.core:core:1.18.0"
    implementation "androidx.constraintlayout:constraintlayout:2.2.1"
    implementation "androidx.games:games-activity:4.4.2"
    implementation "androidx.games:games-frame-pacing:2.1.3"
}
