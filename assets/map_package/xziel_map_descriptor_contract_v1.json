{
  "schemaVersion": 1,
  "format": "xziel_map_descriptor_contract_v1",
  "requiredFile": "xziel.map.json",
  "requiredFields": {
    "schemaVersion": 1,
    "format": "xziel_map_descriptor_v1",
    "mapId": {
      "pattern": "^[a-z0-9][a-z0-9_]{0,62}$"
    },
    "displayName": {
      "minLength": 1,
      "maxLength": 128
    },
    "entryWorld": {
      "pattern": "^maps/.+\\.bsp$"
    },
    "gameMode": {
      "allowed": [
        "round_based_zombies"
      ]
    },
    "maxPlayers": {
      "minimum": 1,
      "maximum": 4
    },
    "contentContract": {
      "const": "xziel_map_content_contract_v1"
    },
    "serverAuthoritative": {
      "const": true
    }
  },
  "rules": [
    "entryWorld must exist inside the package payload with exact case",
    "entryWorld must be classified as world_geometry",
    "mapId is the canonical runtime identity and must not be inferred from a filename",
    "no arbitrary startup console commands are accepted from the package descriptor",
    "all payload references remain subject to the universal zero-omission inventory gate"
  ]
}
